@AGENTS.md

# Claude Code note

Use `AGENTS.md` as the single source of durable project guidance. Then read `docs/PROJECT_SPEC.md` before making changes.

When a task is ambiguous, make a reasonable implementation choice, write the assumption in the relevant issue/doc comment, and continue with the smallest safe implementation.
