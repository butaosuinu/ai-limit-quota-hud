{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the QuotaHUD overlay window.",
  "windows": ["overlay"],
  "permissions": ["core:default"]
}
